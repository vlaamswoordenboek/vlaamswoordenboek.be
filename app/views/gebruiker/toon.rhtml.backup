<p>Deze gebruiker is geregistreerd sinds <%= localize( @user.created_at ) %>

<h2>Toevoegingen</h2>

<% if @woorden.size > 0 %>

	<p>Deze gebruiker heeft bijdragen geleverd bij de volgende termen:</p>

	<%= @woorden.map{ |x| link_to( x, :controller => 'definities', :action => 'term', :id => x ) }.join( ', ' ) %>

<% else %>

	<p>Deze gebruiker heeft nog geen bijdragen geleverd aan het Vlaams woordenboek.</p>

<% end %>

<h2>Reacties</h2>

<% if @reactions.size > 0 %>

	<p>Deze gebruiker heeft <%= @reactions.size %> reactie(s) geschreven:</p>

	<% for @reaction in @reactions %>
		<% if @reaction.definition %>
			<h2><a href='/definities/toon/<%=@reaction.definition.id%>'><%= @reaction.definition.word %></a></h2>
			<%= render(:partial => "definities/reaction") %>
		<% end %>
	<% end %>

<% else %>

	<p>Deze gebruiker heeft nog geen reacties geschreven.</p>

<% end %>